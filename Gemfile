source 'https://rubygems.org'

# 使用 github-pages gem 来锁定所有依赖版本
gem 'github-pages', '~> 232'  # 匹配 GitHub Pages 的版本

# 可选：本地开发需要的 gem
group :development do
  gem 'rake'
  gem 'webrick', '~> 1.7'  # Jekyll 4.x 默认需要 webrick，但 3.10.0 不一定需要
end
